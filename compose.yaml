services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

# docker compose up -d
# docker compose down
# docker compose exec postgres psql -U postgres -d mydb

# docker stop pg-local (container_name)
# docker start pg-local (container_name)
# docker rm -f pg-local (container_name)
# docker exec -it pg-local psql -U postgres -d app

# services:
#   postgres:
#     image: postgres:16
#     container_name: pg-local
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: app
#     ports:
#       - "5432:5432"
#     volumes:
#       - ./data:/var/lib/postgresql/data

# services:
#   postgres:
#     image: postgres:16
#     environment:
#       POSTGRES_PASSWORD: postgres
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#
# volumes:
#   pgdata: